# Week 11: S20 + S21 - April 7 & 9 

# SESSION 20 

**You have const items = ['a', 'b', 'c']. What does items.push('d')return?** It returns 4 and the array adds 'd' at the end. 

**You wrote cont colors =['red', 'blue']; = ['green']; and it crashed. How would you add green without crashing?** Its becasue const array cannot be reassigned. Add 'green' by using colors.push('green'). 

**What is the difference between items.indexOf('x') and items.includes('x')?** Of gives the position of 'x' in the array, includes only checks if x is in the array. 

loop challenges 


# SESSION 21 

**for(let i = 0; i < 10; i ++)** Runs 10 times, 9 is the last number. 

**Const nums = [10, 20, 30] for(let i = 0; i < nums length; i++){let sum=0; sum+=nums[i]j}** You have let sum=0 which resets it back to 0. 

**Objects** -> overall representation of complex data types. 

**API -> Application Programming Interface** 
1. you request data 
2. server processes request 
3. server responds with data, usually JSON. 

random word generator 

