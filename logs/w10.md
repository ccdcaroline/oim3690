# Week 10: S18 + S19 - March 31 & April 2

# SESSION 18 

**You wrote const total = price + tax and got "1005" instead of 15. What happened?** One of the variables is a string so it doesn't add it. 

**Your code has const counter = 0 and later counter = counter + 1. It crashed, why?** Const variables cant be changed. 

**Why does 5== "5" return true but 5 === "5" returns false?** == compares values while === compares values and type. One is a number and one is a string. 

Made grade calculator. 

# SESSION 19 

Javascript -> language run on browser. 

**Your wrote function getGrade(score) but calling getGrade(85) returns undefinded, what's the missing function?** The return statement. 

**Your function checks if (score>= 60) first then else if (score >= 90), what would a student who got a 95 get?** It would get 60 because JS stops at the first true condition. 

**What does the arrow function do?** Takes the number x and returns x squared. 

**You wrote let username = prompt("Enter your name); and the user clicked OK without typing anything, does if (username) run?** An empty string is considered false in JS. 

shopping list builder. 

